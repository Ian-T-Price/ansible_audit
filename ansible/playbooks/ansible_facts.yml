- hosts: localhost     #"{{ hosts }}"
  gather_facts: yes
  connection: local

  tasks:

#     Call ansible module "userinfo" with parameter "user={{ user }}"
    - sudodir2:
#      ignore_errors: True   

      register: sudodir
